<%= form_with(model: cookbook) do |form| %>
  <% if cookbook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cookbook.errors.count, "error") %> prohibited this cookbook from being saved:</h2>

      <ul>
        <% cookbook.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

  <div class="field">
    <%= form.label :cuisine %>
    <%= form.text_field :cuisine %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
